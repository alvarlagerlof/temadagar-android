<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".Main.MainActivity">

    <item
        android:id="@+id/add"
        android:title="Föreslå dag eller ge feedback"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_add">
        <menu>
            <item
                android:id="@+id/add_day"
                android:icon="@drawable/ic_action_add_black"
                android:showAsAction="never"
                android:title="Föreslå dag"
                tools:ignore="AppCompatResource" />
            <item
                android:id="@+id/feedback"
                android:icon="@drawable/ic_action_feedback_black"
                android:showAsAction="never"
                android:title="Ge feedback"
                tools:ignore="AppCompatResource" />
        </menu>

    </item>

    <item
        android:id="@+id/search"
        android:title="Sök"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_search" />

    <item
        android:id="@+id/settings"
        android:title="Inställningar"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_settings" />

</menu>
